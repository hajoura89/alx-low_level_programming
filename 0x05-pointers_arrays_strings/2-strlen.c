#include "main.h"

/**
 *_strlen - a function that returns the length of a string.
 *@s: param
 *Return : Length of the string
 */
int _strlen(char *s)
{
	int n;

	n = 0;

	while (s[n] != '\0')
	{
		n++;
	}

	return (i);

}
